# Servidor
PORT=3000
NODE_ENV=development

# Banco de dados MySQL
DB_HOST=mysql-f45033b-dellsalsicha-fd08.e.aivencloud.com
DB_PORT=18343
DB_USER=avnadmin
DB_PASSWORD=AVNS_15ryqoyvUyta7Yj3GcL
DB_NAME=defaultdb

# --- SSL/TLS para bancos gerenciados externos (ex.: Aiven, PlanetScale) ---
# Necessário apenas se o provedor exigir conexão criptografada.
# Use UMA das duas opções abaixo (path OU content), nunca as duas ao mesmo tempo.
#
# Opção 1: caminho para o arquivo .pem baixado no painel do provedor (uso local/dev)
DB_SSL_CA_PATH=./src/config/ca.pem

# JWT
JWT_SECRET=troque_esta_chave_secreta_super_forte
JWT_EXPIRES_IN=8h

# Usuário admin criado pelo script de seed
ADMIN_NAME=Administrador do Sistema
ADMIN_EMAIL=admin@lms-bncc.edu.br
ADMIN_PASSWORD=Admin@12345
